name: "Cloud PC Setup (Windows / macOS / Linux)"

on:
  workflow_dispatch:
    inputs:
      os_choice:
        description: "Select the Operating System to deploy"
        type: choice
        options:
          - Windows
          - macOS
          - Linux
        required: true
        default: Windows
      
      windows_runner:
        description: "[Windows] Select Runner Model"
        type: choice
        options:
          - windows-latest
          - windows-2025
          - windows-2022
        required: false
        default: windows-2022
      
      macos_runner:
        description: "[macOS] Select Runner Model"
        type: choice
        options:
          - macos-latest
          - macos-15
          - macos-14
          - macos-15-intel
          - macos-13-intel
        required: false
        default: macos-15
      
      linux_runner:
        description: "[Linux] Select Runner Model"
        type: choice
        options:
          - ubuntu-latest
          - ubuntu-24.04
          - ubuntu-22.04
          - ubuntu-24.04-arm64
          - ubuntu-22.04-arm64
        required: false
        default: ubuntu-latest
      
      CODE:
        description: "[Windows] Full headless CRD command (CMD version)"
        required: false
        default: ""
      
      PIN:
        description: "[Windows] CRD PIN (6+ digits)"
        required: false
        default: "123456"
      
      LINUX_CODE:
        description: "[Linux] Full headless CRD command (from Chrome Remote Desktop setup)"
        required: false
        default: ""
      
      LINUX_CRD_PIN:
        description: "[Linux] Chrome Remote Desktop PIN (6+ digits)"
        required: false
        default: "123456"
      
      RUSTDESK_PASSWORD:
        description: "[macOS] Custom RustDesk password"
        required: false
        default: "password123"

concurrency:
  group: cloud-pc-${{ github.ref }}-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  # ==========================================
  # WINDOWS JOB (Runs only if Windows is selected)
  # ==========================================
  windows-crd-setup:
    name: "🚀 Windows CRD Runner"
    if: github.event.inputs.os_choice == 'Windows'
    runs-on: ${{ github.event.inputs.windows_runner }}

    env:
      RAW_CODE: ${{ github.event.inputs.CODE }}
      PIN_INPUT: ${{ github.event.inputs.PIN }}

    defaults:
      run:
        shell: pwsh

    steps:
      - name: "📋 Display Runner Info"
        run: |
          Write-Host "=========================================="
          Write-Host "🖥️  Runner: ${{ github.event.inputs.windows_runner }}"
          Write-Host "💻 OS: Windows"
          Write-Host "=========================================="

      - name: "🛑 Verify Inputs"
        run: |
          if ([string]::IsNullOrWhiteSpace($env:RAW_CODE)) {
            Write-Error "❌ The CRD headless CODE is required for Windows deployments. Please trigger the workflow again and provide the code."
            exit 1
          }

      - name: "🔊 Enable Windows Audio Service"
        run: |
          Write-Host "Setting Windows Audio Service to Automatic..."
          Set-Service -Name Audiosrv -StartupType Automatic
          
          Write-Host "Starting Windows Audio Service..."
          Start-Service -Name Audiosrv
          
          Write-Host "✅ Audio service is running!"

      - name: "🔧 Download & Install Software"
        run: |
          $ErrorActionPreference = 'Stop'
          $userDownloads = Join-Path $env:USERPROFILE 'Downloads'
          if (-not (Test-Path $userDownloads)) { New-Item -ItemType Directory -Path $userDownloads | Out-Null }
          
          # 1. Download Files
          Write-Host "Downloading Chrome Remote Desktop..."
          Invoke-WebRequest -Uri "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi" -OutFile "$userDownloads\crdhost.msi" -UseBasicParsing
          
          Write-Host "Downloading Virtual Audio Cable..."
          Invoke-WebRequest -Uri "https://download.vb-audio.com/Download_CABLE/VBCABLE_Driver_Pack45.zip" -OutFile "$userDownloads\VBCABLE.zip" -UseBasicParsing

          # 2. Extract and Install Virtual Audio Cable
          Write-Host "Extracting Virtual Audio Cable..."
          Expand-Archive -Path "$userDownloads\VBCABLE.zip" -DestinationPath "$userDownloads\VBCABLE" -Force
          
          Write-Host "Installing Virtual Audio Cable..."
          Start-Process -Wait -FilePath "$userDownloads\VBCABLE\VBCABLE_Setup_x64.exe" -ArgumentList "-i", "-h" -Verb RunAs

          # 3. Install Chrome Remote Desktop Silently
          Write-Host "Installing Chrome Remote Desktop..."
          Start-Process -Wait -FilePath "msiexec.exe" -ArgumentList "/i `"$userDownloads\crdhost.msi`" /qn /norestart"
          
          Write-Host "✅ Core software and sound drivers installed."

      - name: "🌟 Start Chrome Remote Desktop"
        run: |
          Write-Host "Executing Google Headless Code..."
          $crdCommand = "$env:RAW_CODE --pin=$env:PIN_INPUT"
          
          try {
              cmd.exe /c $crdCommand
              Write-Host "✅ Chrome Remote Desktop started successfully!"
          } catch {
              Write-Error "❌ Failed to start CRD: $($_.Exception.Message)"
          }

      - name: "🕵️‍♂️ Download GoLogin"
        run: |
          $userDownloads = Join-Path $env:USERPROFILE 'Downloads'
          Write-Host "Downloading GoLogin installer..."
          Invoke-WebRequest -Uri "https://dl.gologin.com/gologin.exe" -OutFile "$userDownloads\gologin.exe" -UseBasicParsing
          
          Write-Host "✅ GoLogin installer is ready in the Downloads folder!"

      - name: "⏳ Keep Runner Alive"
        run: |
          Write-Host "==============================================="
          Write-Host "💻 Your Windows Cloud PC is ready!"
          Write-Host "🔗 Go to: https://remotedesktop.google.com/access"
          Write-Host "🔊 Virtual Sound is enabled!"
          Write-Host "🕵️‍♂️ GoLogin installer is waiting in Downloads."
          Write-Host "⏳ Keeping the server alive for up to 6 hours..."
          Write-Host "==============================================="
          
          while ($true) { 
              Start-Sleep -Seconds 300 
          }

  # ==========================================
  # MACOS JOB (Runs only if macOS is selected)
  # ==========================================
  macos-rustdesk-setup:
    name: "🍏 macOS RustDesk Runner"
    if: github.event.inputs.os_choice == 'macOS'
    runs-on: ${{ github.event.inputs.macos_runner }}

    steps:
      - name: "📋 Display Runner Info"
        run: |
          echo "=========================================="
          echo "🖥️  Runner: ${{ github.event.inputs.macos_runner }}"
          echo "💻 OS: macOS"
          echo "=========================================="

      - name: "🛑 Verify Inputs"
        run: |
          if [ -z "${{ github.event.inputs.RUSTDESK_PASSWORD }}" ]; then
            echo "❌ RustDesk password is required for macOS deployments."
            exit 1
          fi

      - name: "🔓 Create Admin Account for App Installs"
        run: |
          echo "Creating a new admin user because the default runner password is locked..."
          sudo sysadminctl -addUser macadmin -fullName "Mac Admin" -password "${{ github.event.inputs.RUSTDESK_PASSWORD }}" -admin
          
          echo "======================================================"
          echo "✅ Admin Account Created!"
          echo "When macOS prompts for a password to install apps, use:"
          echo "Username: macadmin"
          echo "Password: ${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "======================================================"

      - name: "🚫 Disable Sleep & Screen Lock"
        run: |
          echo "Disabling system sleep..."
          sudo pmset -a sleep 0
          sudo pmset -a displaysleep 0
          sudo pmset -a disksleep 0
          
          echo "Disabling screen lock..."
          defaults write com.apple.screensaver askForPassword -int 0
          defaults write com.apple.screensaver askForPasswordDelay -int 0
          defaults write com.apple.screensaver idleTime -int 0
          
          echo "✅ Sleep and screen lock disabled!"

      - name: "🦀 Install RustDesk"
        run: |
          brew install --cask --no-quarantine rustdesk
          echo "✅ RustDesk installed"

      - name: "🖥️ Install Chrome Remote Desktop"
        run: |
          echo "Downloading Chrome Remote Desktop..."
          curl -L -o /tmp/crd.dmg https://dl.google.com/chrome-remote-desktop/chromeremotedesktop.dmg
          
          echo "Mounting DMG..."
          sudo hdiutil attach /tmp/crd.dmg -nobrowse
          
          echo "Installing Package..."
          PKG_PATH=$(find /Volumes/Chrome\ Remote\ Desktop* -name "*.pkg" | head -n 1)
          sudo installer -pkg "$PKG_PATH" -target /
          
          echo "Unmounting DMG..."
          sudo hdiutil detach /Volumes/Chrome\ Remote\ Desktop* -force
          
          echo "✅ Chrome Remote Desktop installed successfully"

      - name: "🔓 Grant Screen Recording & Accessibility Permissions"
        run: |
          TCC_DB="/Library/Application Support/com.apple.TCC/TCC.db"
          
          RUSTDESK="com.carriez.rustdesk"
          CRD_ASSIST="com.google.chrome.remote_desktop.remote-assistance-host-v2"
          CRD_HOST="com.google.chromeremotedesktop.me2me-host"
          
          SQL_QUERY="
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceScreenCapture', '$RUSTDESK', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceAccessibility', '$RUSTDESK', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceScreenCapture', '$CRD_ASSIST', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceAccessibility', '$CRD_ASSIST', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceScreenCapture', '$CRD_HOST', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceAccessibility', '$CRD_HOST', 0, 2, 4, 1, 0);
          "
          
          sudo sqlite3 "$TCC_DB" "$SQL_QUERY"
          echo "✅ Permissions injected into TCC.db for RustDesk and Chrome Remote Desktop"

      - name: "🖥️ Initialize Virtual Display"
        run: |
          sudo defaults write /Library/Preferences/com.apple.windowserver.plist DisplayResolutionEnabled -bool YES
          echo "✅ Virtual display settings initialized"

      - name: "🖼️ Set Desktop Wallpaper"
        run: |
          echo "Fetching a standard high-res wallpaper..."
          curl -L -o /Users/runner/wallpaper.jpg "https://images.unsplash.com/photo-1506744626753-dba37c2a9816?q=80&w=2560&auto=format&fit=crop"
          
          echo "Applying wallpaper via AppleScript..."
          osascript -e 'tell application "Finder" to set desktop picture to POSIX file "/Users/runner/wallpaper.jpg"'
          
          echo "✅ Desktop wallpaper updated (no more black screen)!"

      - name: "🕵️‍♂️ Download & Install MostLogin"
        run: |
          echo "Downloading MostLogin..."
          curl -L -o /tmp/mostlogin.dmg "https://downsoft.mostlogin.com/MostLogin-x64-setup.dmg"
          
          echo "Mounting DMG..."
          MOUNT_DIR=$(sudo hdiutil attach /tmp/mostlogin.dmg -nobrowse | grep -o '/Volumes/.*')
          
          APP_PATH=$(find "$MOUNT_DIR" -maxdepth 1 -name "*.app" | head -n 1)
          APP_NAME=$(basename "$APP_PATH")
          
          echo "Installing $APP_NAME..."
          sudo cp -R "$APP_PATH" /Applications/
          
          echo "Bypassing Gatekeeper Quarantine for $APP_NAME..."
          sudo xattr -rd com.apple.quarantine "/Applications/$APP_NAME" || true
          
          echo "Unmounting DMG..."
          sudo hdiutil detach "$MOUNT_DIR" -force
          
          echo "✅ $APP_NAME installed to Applications folder!"

      - name: "🕵️‍♂️ Download & Install GoLogin (ARM64)"
        run: |
          echo "Downloading GoLogin..."
          curl -L -o /tmp/gologin.dmg "https://dl.gologin.com/gologin-arm64.dmg"
          
          echo "Mounting DMG..."
          MOUNT_DIR=$(sudo hdiutil attach /tmp/gologin.dmg -nobrowse | grep -o '/Volumes/.*')
          
          APP_PATH=$(find "$MOUNT_DIR" -maxdepth 1 -name "*.app" | head -n 1)
          APP_NAME=$(basename "$APP_PATH")
          
          echo "Installing $APP_NAME..."
          sudo cp -R "$APP_PATH" /Applications/
          
          echo "Bypassing Gatekeeper Quarantine for $APP_NAME..."
          sudo xattr -rd com.apple.quarantine "/Applications/$APP_NAME" || true
          
          echo "Unmounting DMG..."
          sudo hdiutil detach "$MOUNT_DIR" -force
          
          echo "✅ $APP_NAME installed to Applications folder!"

      - name: "🚀 Start RustDesk"
        run: |
          RUSTDESK_APP=$(find /Applications -name "RustDesk.app" -type d | head -1)
          open "$RUSTDESK_APP"
          "$RUSTDESK_APP/Contents/MacOS/rustdesk" --password "${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          
          echo "✅ RustDesk started with custom password"

      - name: "📸 Capture ID & Password"
        run: |
          echo "⏳ Waiting 20s for RustDesk to generate ID..."
          sleep 20
          screencapture -x /tmp/rustdesk-id.png
          
          if [ -f "/tmp/rustdesk-id.png" ]; then
            echo "✅ Screenshot captured successfully"
          else
            echo "❌ Failed to take screenshot"
            exit 1
          fi

      - name: "🌐 Upload Screenshot to Cloud (Direct Link)"
        run: |
          echo "Uploading screenshot to free hosting..."
          IMAGE_URL=$(curl -s -F "reqtype=fileupload" -F "fileToUpload=@/tmp/rustdesk-id.png" https://catbox.moe/user/api.php)
          
          if [[ $IMAGE_URL == http* ]]; then
            echo "✅ Upload successful!"
            echo "IMAGE_URL=$IMAGE_URL" >> $GITHUB_ENV
          else
            echo "❌ Upload failed. Falling back to Artifacts."
            echo "IMAGE_URL=Upload failed, please download the artifact zip from the Summary page." >> $GITHUB_ENV
          fi

      - name: "📋 Upload Credentials (Backup)"
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-credentials
          path: /tmp/rustdesk-id.png
          retention-days: 1

      - name: "⏳ Connection Window"
        run: |
          echo "🚀 STATUS: READY TO CONNECT!"
          echo "------------------------------------------------------"
          echo "📸 VIEW YOUR ID HERE: ${{ env.IMAGE_URL }}"
          echo "🔑 Your Password is: ${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "------------------------------------------------------"
          echo "💡 PRO TIP: Want to switch to the clean macadmin desktop?"
          echo "   1. Connect via RustDesk"
          echo "   2. Click the Apple Logo  -> Lock Screen"
          echo "   3. Log in as 'macadmin' with your password"
          echo "------------------------------------------------------"
          
          while true; do sleep 3600; done

  # ==========================================
  # LINUX JOB (Runs only if Linux is selected)
  # ==========================================
  linux-crd-setup:
    name: "🐧 Linux CRD Runner"
    if: github.event.inputs.os_choice == 'Linux'
    runs-on: ${{ github.event.inputs.linux_runner }}

    steps:
      - name: "📋 Display Runner Info"
        run: |
          echo "=========================================="
          echo "🖥️  Runner: ${{ github.event.inputs.linux_runner }}"
          echo "💻 OS: Linux (Ubuntu)"
          echo "=========================================="

      - name: "🛑 Verify Inputs"
        run: |
          if [ -z "${{ github.event.inputs.LINUX_CODE }}" ]; then
            echo "❌ The CRD headless CODE is required for Linux deployments."
            echo "Please trigger the workflow again and provide the code from Chrome Remote Desktop setup."
            exit 1
          fi

      - name: "🔄 Update System Packages"
        run: |
          echo "Updating system packages..."
          sudo apt-get update -y
          sudo apt-get upgrade -y
          echo "✅ System packages updated!"

      - name: "📦 Install Required Dependencies"
        run: |
          echo "Installing system dependencies..."
          sudo apt-get install -y \
            curl \
            wget \
            unzip \
            xvfb \
            x11-utils \
            pulseaudio \
            pavucontrol \
            alsa-utils \
            mesa-utils \
            x11-apps \
            gnome-shell \
            gnome-control-center \
            dbus-x11 \
            systemd \
            lightdm \
            python3-pip \
            git
          
          echo "✅ All dependencies installed!"

      - name: "🔊 Setup Audio Service"
        run: |
          echo "Configuring PulseAudio..."
          sudo usermod -a -G audio $USER || true
          
          echo "Starting PulseAudio daemon..."
          pulseaudio --daemonize --log-target=syslog || true
          
          echo "✅ Audio configured!"

      - name: "🖥️ Setup Display Environment"
        run: |
          echo "Creating virtual display..."
          export DISPLAY=:99
          
          echo "Starting Xvfb (virtual framebuffer)..."
          sudo Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
          
          sleep 2
          
          echo "✅ Virtual display :99 is running!"
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: "🔐 Create User Account & Sudoers"
        run: |
          echo "Creating cloudpc user..."
          sudo useradd -m -s /bin/bash -G sudo,audio,video cloudpc || true
          echo "cloudpc:cloudpc123" | sudo chpasswd
          
          echo "Adding cloudpc to sudoers..."
          echo "cloudpc ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/cloudpc > /dev/null
          
          echo "✅ User account created!"

      - name: "🌐 Download & Install Chrome Remote Desktop"
        run: |
          echo "Determining system architecture..."
          ARCH=$(uname -m)
          
          if [ "$ARCH" = "aarch64" ]; then
            ARCH_TYPE="arm64"
          elif [ "$ARCH" = "x86_64" ]; then
            ARCH_TYPE="amd64"
          else
            ARCH_TYPE="amd64"
          fi
          
          echo "System architecture: $ARCH_TYPE"
          
          echo "Downloading Chrome Remote Desktop..."
          CRD_URL="https://dl.google.com/linux/direct/chrome-remote-desktop_current_${ARCH_TYPE}.deb"
          
          wget -q "$CRD_URL" -O /tmp/crd.deb
          
          echo "Installing Chrome Remote Desktop..."
          sudo dpkg -i /tmp/crd.deb || sudo apt-get install -f -y
          
          echo "✅ Chrome Remote Desktop installed!"

      - name: "🔧 Configure Chrome Remote Desktop"
        run: |
          echo "Creating CRD configuration directory..."
          sudo mkdir -p /etc/chrome-remote-desktop
          
          echo "Configuring CRD host..."
          sudo tee /etc/chrome-remote-desktop/chrome-remote-desktop.conf > /dev/null <<EOF
          DISPLAY=:99
          PULSE_AUDIOSERVER=/run/user/\$(id -u)/pulse/native
          LD_LIBRARY_PATH=/opt/google/chrome:/opt/google/chrome/lib:\$LD_LIBRARY_PATH
          EOF
          
          echo "✅ Chrome Remote Desktop configured!"

      - name: "🚀 Start Chrome Remote Desktop"
        run: |
          echo "Starting Chrome Remote Desktop with headless command..."
          
          export DISPLAY=:99
          
          LINUX_CRD_CMD="${{ github.event.inputs.LINUX_CODE }}"
          
          echo "Executing CRD command..."
          eval $LINUX_CRD_CMD &
          
          sleep 10
          
          if pgrep -f "chrome_remote_desktop" > /dev/null; then
            echo "✅ Chrome Remote Desktop is running!"
          else
            echo "⚠️  CRD may still be starting up..."
          fi

      - name: "📸 Capture Desktop & Connection Info"
        run: |
          echo "Waiting for services to fully start (20s)..."
          sleep 20
          
          export DISPLAY=:99
          
          echo "Capturing desktop screenshot..."
          import -window root /tmp/linux-desktop.png 2>/dev/null || \
          XVFBSCREEN=:99 scrot /tmp/linux-desktop.png 2>/dev/null || \
          echo "Screenshot utility not available, but server is running"
          
          echo "✅ Desktop capture attempted!"

      - name: "📋 Upload Screenshot (Backup)"
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: linux-desktop-screenshot
          path: /tmp/linux-desktop.png
          retention-days: 1

      - name: "📊 Display Connection Information"
        run: |
          echo "=========================================="
          echo "🚀 STATUS: LINUX CLOUD PC READY!"
          echo "=========================================="
          echo ""
          echo "📌 CONNECTION METHOD:"
          echo ""
          echo "1️⃣  CHROME REMOTE DESKTOP:"
          echo "   🔗 Go to: https://remotedesktop.google.com/access"
          echo "   📌 PIN: ${{ github.event.inputs.LINUX_CRD_PIN }}"
          echo ""
          echo "🖥️  SYSTEM INFO:"
          echo "   🏗️  Architecture: $(uname -m)"
          echo "   📦 OS: $(cat /etc/os-release | grep PRETTY_NAME | cut -d'"' -f2)"
          echo "   💻 Display: :99"
          echo "   👤 Default User: root"
          echo "   👤 Created User: cloudpc (password: cloudpc123)"
          echo ""
          echo "=========================================="
          echo "⏳ Keeping server alive for 6 hours..."
          echo "=========================================="

      - name: "⏳ Keep Runner Alive"
        run: |
          echo "Server is now live and accepting remote connections..."
          echo "Idle timeout: 6 hours"
          
          while true; do
            sleep 300
            echo "[$(date)] Server still running..."
          done
